/**
 * Layout server configuration
 * This file handles server-side page options
 */

/**
 * Enable prerendering for all pages
 * This generates static HTML at build time for better performance
 */
export const prerender = true;
